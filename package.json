{
  "name": "collabchat-board",
  "version": "0.1.0",
  "description": "Real-time collaborative chat and whiteboard",
  "private": true,
  "workspaces": [
    "client",
    "server",
    "packages/shared"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace=server\" \"npm run dev --workspace=client\"",
    "build": "npm run build --workspace=packages/shared && npm run build --workspace=server && npm run build --workspace=client",
    "build:server": "npm run build --workspace=packages/shared && npm run build --workspace=server",
    "build:client": "npm run build --workspace=packages/shared && npm run build --workspace=client",
    "start": "npm start --workspace=server",
    "lint": "eslint . --ext .ts,.tsx",
    "typecheck": "npx tsc -p packages/shared && npx tsc --noEmit -p server && npx tsc --noEmit -p client"
  },
  "devDependencies": {
    "concurrently": "^8.2.0",
    "typescript": "6.0.0-dev.20260401"
  }
}
